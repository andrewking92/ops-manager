This is a sample Ops Manager deployment on AWS using Terraform and Ansible.

Initial Setup

export AWS_PROFILE
export ANSIBLE_CONFIG

Generate a keypair from AWS


terraform init
terraform plan
terraform apply
